#pragma once

#include "zc_mem.h"
#include "zc_dynamic_array.h"

namespace zf {
    /*class c_binary_tree {
    public:
        struct s_node {
            int val;

            s_node* left;
            s_node* right;
        };

        void Insert(const int val, c_mem_arena& mem_arena) {
            s_node*& n = m_root;

            while (n) {
                if (val <= n->val) {
                    n = n->left;
                } else {
                    n = n->right;
                }
            }
        }

    private:
        s_node* m_root;

        c_mem_arena& m_mem_arena;
    };

    class c_avl_tree {
    };*/
}
